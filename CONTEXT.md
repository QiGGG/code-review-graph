# code-review-graph — lineage 领域术语

围绕「函数血缘（lineage）追踪与代码规模统计」功能扩展建立的统一语言。范围：评估改动影响面、统计单个函数/文件/文件夹的血缘覆盖代码量。

## Language

**血缘 (lineage)**:
从某个函数出发、沿 CALLS 调用边可达的函数集合；血缘总是双向的——上游 callers（谁调用了它）+ 下游 callees（它调用了谁）+ 该函数自身。
_Avoid_: 调用图、依赖树、被调用者列表

**上游 (upstream callers)**:
调用血缘根函数的那一侧函数集合。
_Avoid_: 父函数、引用方

**下游 (downstream callees)**:
血缘根函数所调用的那一侧函数集合。
_Avoid_: 子函数、被引用方

**血缘覆盖 (lineage coverage)**:
血缘集合内各函数（按身份去重后）对应代码行的总和。以函数为计数单位，一个函数无论被多少条路径到达都只贡献一次行数。
_Avoid_: 波及行、命中代码

**统计作用域 (scope)**:
用户本次分析指定的边界，可以是单个文件或一个文件夹（递归含整棵子树）。作用域只决定「哪些函数入根」，不限制血缘覆盖的延伸范围。
_Avoid_: 包、目录过滤器、范围参数

**根函数 (root function)**:
统计作用域内、作为血缘展开起点的每个函数。聚合时根集合 = 作用域内全部符合纳入规则的函数。
_Avoid_: 目标函数、种子函数

**函数身份 (function identity)**:
判断「两个函数是否同一个」的唯一依据，即图节点键 `qualified_name`。它内嵌规范化文件路径（`文件路径::[类名.]函数名`），因此不同文件中的同名函数天然是不同身份。
_Avoid_: 函数名、裸名字、符号名

**函数类节点 (function-like node)**:
按 `qualified_name` 身份聚合与统计行数时纳入的节点种类：Function 与 Test（类方法在图中即为带 `parent_name` 的 Function/Test；Class 节点本身不计入行数）。
_Avoid_: 方法、函数定义节点

**函数级聚合 (function-level aggregation)**:
以「统计作用域内所有函数类节点」为根集合，各自展开完整血缘，再按函数身份去重后合并计数。它是单函数血缘在「多根」上的推广；根集合可含函数体内嵌套 def，其既有身份局限由建图层负责，聚合层不做二次处理。
_Avoid_: 批量血缘、合计血缘

**作用域聚合覆盖 (scope aggregate coverage)**:
对一次函数级聚合的结果，按函数行数口径（`行尾 - 行首 + 1`）求和得到的去重后总行数与总函数数，以及按文件归并的明细。仅指 CLI 文本输出中的统计结论。
_Avoid_: 波及行、覆盖体积

**嵌套函数身份局限 (nested-function identity limitation)**:
已知建图层局限：非 Julia/C++ 语言的函数体内嵌套 def，其身份键不嵌入外层函数名，导致同文件内同名嵌套 def 会被合并成单节点。本次聚合功能不处理该局限，仅记录在案。
_Avoid_: 缺陷、bug（在领域语言层面不称为缺陷，而是已知建模边界）
